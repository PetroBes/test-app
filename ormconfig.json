{
  "name": "default",
  "type": "postgres",
  "host": "postgres",
  "username": "auth-test",
  "password": "test",
  "database": "auth-db",
  "port": 5432,
  "logging": false,
  "entities": ["src/**/*.entity.ts"],
  "migrations": ["src/migrations/**/*.ts"],
  "migrationsRun": true,
  "synchronize": false,
  "cli": {
    "entitiesDir": "src",
    "migrationsDir": "migrations"
  }
}
